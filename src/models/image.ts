export type ImageModel = {
    id: string
    description: string
    likes: number
    url: string // testing
    urls: {
        regular: string
        // small?: string
        // thumb?: string
    }
}
